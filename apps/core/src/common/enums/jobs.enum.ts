export enum EmailJobs {
  SEND_PASSWORD_RESET_EMAIL = 'send-password-reset-email',
  SEND_EMAIL_CHANGE_CONFIRMATION = 'send-email-change-confirmation',
}

export enum AiTransactionJobs {
  PROCESS_AI_TRANSACTION = 'process-ai-transaction',
}

export enum ScheduledTransactionJobs {
  EXECUTE_SCHEDULED_RULES = 'execute-scheduled-rules',
}
